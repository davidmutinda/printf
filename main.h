#ifndef _MAIN_H_
#define _MAIN_H_
#include <stdarg.h>

int _printf(const char *format, ...);
int _putchar(char c);
int switch_statement(const char *format, int i, va_list ap);
void print_numbers(char *); /* function to print numbers, to be written */

#endif
